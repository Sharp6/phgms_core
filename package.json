{
  "name": "phgms_core",
  "version": "1.0.0",
  "description": "The core module for PHGMS",
  "main": "index.js",
  "scripts": {
    "test": "mocha -u bdd -R spec -t 500 --recursive",
    "watch": "mocha -u bdd -R spec -t 500 --recursive --watch"
  },
  "keywords": [
    "phgms"
  ],
  "author": "Philip Huysmans",
  "license": "ISC",
  "dependencies": {
    "moment": "^2.13.0",
    "mqtt": "^1.10.0"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "chai-as-promised": "^5.3.0",
    "mocha": "^2.4.5",
    "sinon": "^1.17.4",
    "sinon-chai": "^2.8.0"
  }
}
